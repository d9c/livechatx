export type ThemeType = {
  title: string;
  background: string;
  text: string;
  border: {
    main: string;
    hover: string;
  };
  button: {
    main: string;
    hover: string;
  };
  message: {
    sent: string;
    received: string;
  };
};
