export type ThemeType = {
  title: string;
  background: string;
  container: {
    header: string;
    body: string;
  };
  text: string;
  border: string;
  message: {
    sent: string;
    received: string;
  };
};
